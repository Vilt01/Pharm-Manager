<UserControl x:Class="TaskManager.Views.UsersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:TaskManager.Views"
             Background="White">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" BorderBrush="#E0E0E0" BorderThickness="0,0,1,0" Padding="15">
            <StackPanel>
                <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         materialDesign:HintAssist.Hint="Поиск"
                         materialDesign:TextFieldAssist.HasClearButton="True"
                         Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                         Margin="0,0,0,20" FontSize="14"/>

                <Button Content="Выбрать все" 
                        Command="{Binding ClearFilterCommand}"
                        Style="{StaticResource MaterialDesignFlatButton}"
                        HorizontalAlignment="Left" Margin="0,0,0,10"
                        Foreground="Black" FontSize="14"/>

                <ListBox ItemsSource="{Binding Roles}" 
                         SelectedItem="{Binding SelectedRole}"
                         BorderThickness="0" Background="Transparent">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Name}" FontSize="14" Padding="5,10" Foreground="Gray"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </StackPanel>
        </Border>

        <Grid Grid.Column="1" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="Справочник пользователей" FontSize="18" FontWeight="Bold" VerticalAlignment="Center"/>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Command="{Binding EditCommand}"
                            Style="{StaticResource MaterialDesignFloatingActionMiniLightButton}"
                            Margin="0,0,10,0" ToolTip="Редактировать выделенного">
                        <materialDesign:PackIcon Kind="Pencil" Width="20" Height="20"/>
                    </Button>

                    <Button Command="{Binding DeleteCommand}"
                            Background="#FF5555" BorderBrush="#FF5555" Foreground="White"
                            Style="{StaticResource MaterialDesignFloatingActionMiniButton}"
                            Margin="0,0,10,0" ToolTip="Удалить выбранных">
                        <materialDesign:PackIcon Kind="Delete" Width="20" Height="20"/>
                    </Button>

                    <Button Command="{Binding AddCommand}"
                            Style="{StaticResource MaterialDesignFloatingActionMiniLightButton}"
                            ToolTip="Добавить пользователя">
                        <materialDesign:PackIcon Kind="Plus" Width="24" Height="24"/>
                    </Button>
                </StackPanel>
            </Grid>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding UsersView}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <materialDesign:Card Width="220" Margin="0,0,20,20" Padding="15" UniformCornerRadius="8" Background="#F5F5F5">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <CheckBox Grid.Row="0" HorizontalAlignment="Left" 
                                              IsChecked="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}"
                                              Style="{StaticResource MaterialDesignUserForegroundCheckBox}" />

                                    <materialDesign:PackIcon Grid.Row="1" Kind="AccountOutline" 
                                                             Width="60" Height="60" 
                                                             Foreground="Gray"
                                                             HorizontalAlignment="Center" Margin="0,5,0,10"/>

                                    <TextBlock Grid.Row="2" Text="{Binding FullName}" 
                                               FontSize="14" FontWeight="SemiBold" TextAlignment="Center" TextWrapping="Wrap" Margin="0,0,0,3"/>

                                    <TextBlock Grid.Row="3" Text="{Binding Mail}" 
                                               Foreground="#2196F3" FontSize="11" TextAlignment="Center" Margin="0,0,0,3"/>

                                    <TextBlock Grid.Row="4" Text="{Binding Role.Name}" 
                                               Foreground="Black" FontSize="12" FontWeight="Medium" TextAlignment="Center" Margin="0,0,0,5"/>

                                    <StackPanel Grid.Row="5" Opacity="0.7">
                                        <TextBlock Text="{Binding Login, StringFormat='Логин: {0}'}" FontSize="10" TextAlignment="Center"/>
                                        <TextBlock Text="{Binding NumberPhone, StringFormat='Тел: {0}'}" FontSize="10" TextAlignment="Center"/>
                                    </StackPanel>
                                </Grid>
                            </materialDesign:Card>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>

        <Grid Grid.ColumnSpan="2" Visibility="{Binding IsModalOpen, Converter={StaticResource BoolToVis}}">
            <local:CreateUserView DataContext="{Binding CreateUserVM}"/>
        </Grid>

    </Grid>
</UserControl>