<UserControl x:Class="TaskManager.Views.OrdersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TaskManager.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="#FAFAFA">

    <UserControl.Resources>
        <Style x:Key="AestheticBtn" TargetType="Button">
            <Setter Property="Width" Value="40"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="#555555"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontFamily" Value="Segoe UI Symbol"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="20" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#F7F7F7"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#EEEEEE"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Left">
                <Button Style="{StaticResource AestheticBtn}" Content="+" FontSize="24" Padding="0,0,0,4"/>
                <Button Style="{StaticResource AestheticBtn}" Content="✎"/>
                <Button Style="{StaticResource AestheticBtn}" Content="✕" Foreground="#FF5555" FontSize="16"/>
            </StackPanel>

            <Border Grid.Column="1" Width="450" Background="White" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="20" Padding="5">
                <Grid>
                    <TextBlock x:Name="SearchPlaceholder" 
                               Text="Поиск..." 
                               Foreground="#AAAAAA" 
                               VerticalAlignment="Center" 
                               Margin="8,0,0,0"
                               IsHitTestVisible="False">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Visible"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Text.Length, ElementName=SearchBox, Mode=OneWay}" Value="0">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding Text.IsEmpty, ElementName=SearchBox, Mode=OneWay}" Value="False">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>

                    <TextBox x:Name="SearchBox" 
                             BorderThickness="0" 
                             Background="Transparent"
                             VerticalAlignment="Center" 
                             Padding="5,0" 
                             Foreground="#333"
                             KeyDown="SearchBox_KeyDown"/>
                </Grid>
            </Border>
        </Grid>

        <DataGrid x:Name="OrdersGrid" 
                  Grid.Row="1" 
                  ItemsSource="{Binding Requests}" 
                  AutoGenerateColumns="False"
                  CanUserAddRows="False" 
                  CanUserDeleteRows="False"
                  CanUserSortColumns="True"
                  BorderThickness="0"
                  Background="White"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column"
                  IsReadOnly="True">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Название" Binding="{Binding name}" Width="*" CanUserSort="False"/>

                <DataGridTextColumn Header="Номер заказа" Binding="{Binding number_order}" Width="150" CanUserSort="True"/>

                <DataGridTextColumn Binding="{Binding status_requestion}" Width="150" CanUserSort="False">
                    <DataGridTextColumn.HeaderTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="Статус" VerticalAlignment="Center" Margin="0,0,5,0"/>

                                <ToggleButton x:Name="FilterBtn" Grid.Column="1" Width="20" Height="20" Background="Transparent" BorderThickness="0">
                                    <ToggleButton.Content>
                                        <Path Data="M 0 0 L 4 4 L 8 0 Z" Fill="#777" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                    </ToggleButton.Content>
                                </ToggleButton>

                                <Popup IsOpen="{Binding IsChecked, ElementName=FilterBtn}" PlacementTarget="{Binding ElementName=FilterBtn}" StaysOpen="True">
                                    <Border Background="White" BorderBrush="#DDD" BorderThickness="1" Padding="5" CornerRadius="3">
                                        <Border.Effect>
                                            <DropShadowEffect BlurRadius="10" Opacity="0.1"/>
                                        </Border.Effect>
                                        <StackPanel>
                                            <TextBlock Text="Фильтровать:" FontWeight="Bold" Margin="0,0,0,5"/>
                                            <CheckBox Content="Создан" Tag="Создан" IsChecked="True" Click="OnFilterChanged" Margin="0,3" />
                                            <CheckBox Content="В работе" Tag="в работе" IsChecked="True" Click="OnFilterChanged" Margin="0,3" />
                                            <CheckBox Content="Завершен" Tag="Завершен" IsChecked="True" Click="OnFilterChanged" Margin="0,3" />
                                            <CheckBox Content="Отклонен" Tag="Отклонен" IsChecked="True" Click="OnFilterChanged" Margin="0,3" />
                                        </StackPanel>
                                    </Border>
                                </Popup>
                            </Grid>
                        </DataTemplate>
                    </DataGridTextColumn.HeaderTemplate>
                </DataGridTextColumn>

                <DataGridTextColumn Binding="{Binding date_create, StringFormat=dd.MM.yyyy}" Width="150" CanUserSort="False">
                    <DataGridTextColumn.HeaderTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="Дата" VerticalAlignment="Center" Margin="0,0,5,0"/>

                                <ToggleButton x:Name="DateFilterBtn" Grid.Column="1" Width="20" Height="20" Background="Transparent" BorderThickness="0">
                                    <ToggleButton.Content>
                                        <Path Data="M 0 0 L 4 4 L 8 0 Z" Fill="#777" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                    </ToggleButton.Content>
                                </ToggleButton>

                                <Popup IsOpen="{Binding IsChecked, ElementName=DateFilterBtn}" PlacementTarget="{Binding ElementName=DateFilterBtn}" StaysOpen="True">
                                    <Border Background="White" BorderBrush="#DDD" BorderThickness="1" Padding="10" CornerRadius="3">
                                        <Border.Effect>
                                            <DropShadowEffect BlurRadius="10" Opacity="0.1"/>
                                        </Border.Effect>
                                        <StackPanel Width="220">
                                            <TextBlock Text="Быстрый выбор:" FontWeight="Bold" Margin="0,0,0,5"/>

                                            <CheckBox Content="За сегодня" Tag="Today" Click="OnQuickDateFilter" Margin="0,3"/>
                                            <CheckBox Content="За вчера" Tag="Yesterday" Click="OnQuickDateFilter" Margin="0,3"/>
                                            <CheckBox Content="Текущий месяц" Tag="Month" Click="OnQuickDateFilter" Margin="0,3"/>
                                            <CheckBox Content="Прошлый месяц" Tag="LastMonth" Click="OnQuickDateFilter" Margin="0,3"/>

                                            <CheckBox x:Name="AllPeriodCheck" Content="Весь период" IsChecked="True" Click="OnAllPeriodClick" Margin="0,3" FontWeight="SemiBold"/>

                                            <Separator Margin="0,10"/>
                                            <TextBlock Text="Точный диапазон:" FontWeight="Bold" Margin="0,0,0,5"/>

                                            <DatePicker x:Name="DateFrom" Margin="0,2"/>
                                            <TextBlock Text="по" HorizontalAlignment="Center" Foreground="#AAA" Margin="0,2"/>
                                            <DatePicker x:Name="DateTo" Margin="0,2"/>

                                            <Grid Margin="0,10,0,0">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <Button Grid.Column="0" Content="Сброс" Click="ResetDateFilter" Margin="0,0,5,0"/>
                                                <Button Grid.Column="1" Content="Применить" Click="ApplyDateRange_Click" Background="#4CAF50" Foreground="White" BorderThickness="0"/>
                                            </Grid>
                                        </StackPanel>
                                    </Border>
                                </Popup>
                            </Grid>
                        </DataTemplate>
                    </DataGridTextColumn.HeaderTemplate>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>