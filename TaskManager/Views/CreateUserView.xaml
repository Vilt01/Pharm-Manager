<UserControl x:Class="TaskManager.Views.CreateUserView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             Background="#80000000">
    <Grid>
        <materialDesign:Card Width="500" VerticalAlignment="Center" HorizontalAlignment="Center" Padding="30" UniformCornerRadius="10" Background="White">
            <StackPanel>
                <TextBlock Text="Добавление пользователя" FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBox Text="{Binding SurName, UpdateSourceTrigger=PropertyChanged}" materialDesign:HintAssist.Hint="Фамилия *" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Margin="0,0,0,15"/>
                        <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" materialDesign:HintAssist.Hint="Имя *" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Margin="0,0,0,15"/>
                        <TextBox Text="{Binding LastName, UpdateSourceTrigger=PropertyChanged}" materialDesign:HintAssist.Hint="Отчество" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Margin="0,0,0,15"/>
                        <TextBox Text="{Binding NumberPhone, UpdateSourceTrigger=PropertyChanged}" materialDesign:HintAssist.Hint="Телефон *" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Margin="0,0,0,15"/>
                        <TextBox Text="{Binding Mail, UpdateSourceTrigger=PropertyChanged}" materialDesign:HintAssist.Hint="Почта *" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Margin="0,0,0,15"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <TextBox Text="{Binding Login}" materialDesign:HintAssist.Hint="Логин *" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Margin="0,0,0,15"/>
                        <TextBox Text="{Binding Password}" materialDesign:HintAssist.Hint="Пароль *" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Margin="0,0,0,15"/>

                        <ComboBox ItemsSource="{Binding Roles}" SelectedItem="{Binding SelectedRole}" DisplayMemberPath="Name" 
                                  materialDesign:HintAssist.Hint="Роль *" Style="{StaticResource MaterialDesignFloatingHintComboBox}" Margin="0,0,0,15"/>

                        <ComboBox ItemsSource="{Binding Departments}" SelectedItem="{Binding SelectedDepartment}" DisplayMemberPath="Name" 
                                  materialDesign:HintAssist.Hint="Отдел" Style="{StaticResource MaterialDesignFloatingHintComboBox}" Margin="0,0,0,15"/>
                    </StackPanel>
                </Grid>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                    <Button Content="Отмена" Command="{Binding CancelCommand}" Style="{StaticResource MaterialDesignFlatButton}" Margin="0,0,10,0"/>
                    <Button Content="Сохранить" Command="{Binding SaveCommand}" />
                </StackPanel>
            </StackPanel>
        </materialDesign:Card>
    </Grid>
</UserControl>